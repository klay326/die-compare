name: Manual build reminder

on:
  push:
    branches: [main]

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages is serving from /docs folder. Run 'npm run build' locally to update."
